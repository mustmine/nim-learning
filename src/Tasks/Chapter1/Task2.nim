# Check if your age is divisible by 3
let yearAge: int = 9
let division = yearAge mod 3

if division == 0:
    echo "your age can be divisible by 3"
else:
    echo "your age can not be divisible by 3"